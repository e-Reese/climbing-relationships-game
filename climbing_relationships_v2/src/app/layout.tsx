import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Rhythm Heroes - Two Player Rhythm Game',
  description: 'A turn-based rhythm game where players compete to hit the most notes perfectly!',
  keywords: 'rhythm game, guitar hero, two player, turn based, music game',
  authors: [{ name: 'Rhythm Heroes' }],
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
